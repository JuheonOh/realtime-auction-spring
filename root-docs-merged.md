# 루트 문서 요약본

- 문서 성격: 루트 마크다운 문서 통합 요약본
- 기준: 2026-03-04~2026-03-17 사이에 작성된 루트 문서 묶음의 핵심 내용 정리
- 제외 문서: `readme.md`
- 목적: 프로젝트 구조, 현재 검증 범위, 핵심 리스크, 작업 이력을 한 문서에서 빠르게 파악할 수 있게 정리

## 먼저 읽을 부분

- 프로젝트 전체 구조를 먼저 파악하려면 `프로젝트 한눈에`와 `시스템 구성`부터 읽으면 됩니다.
- 현재 무엇이 검증됐는지 알고 싶으면 `핵심 기능`과 `테스트 현황`을 보면 됩니다.
- 기술 리스크와 개선 포인트를 알고 싶으면 `핵심 리스크와 개선 우선순위`를 먼저 보면 됩니다.
- 발표나 면접용 설명 포인트가 필요하면 `설명 포인트`와 `한 줄 요약`을 참고하면 됩니다.

## 프로젝트 한눈에

- 이 프로젝트는 `Spring Boot + React` 기반의 실시간 경매 시스템입니다.
- 실시간 입찰은 `WebSocket`, 사용자 알림은 `SSE`로 분리되어 있습니다.
- 주요 데이터는 `MariaDB`에 저장하고, 실시간 입찰 로그와 일부 인증 데이터는 `Redis`를 사용합니다.
- 즉시구매는 현재 기준으로 `HTTP 요청`으로 처리하고, 결과 전파는 실시간 채널로 전달하는 구조로 정리되어 있습니다.
- 문서 전반의 가장 중요한 기술 리스크는 `입찰 가격 갱신의 원자성 부족`입니다.

## 시스템 구성

| 영역               | 현재 구성                             | 역할                                                 |
| ------------------ | ------------------------------------- | ---------------------------------------------------- |
| 백엔드             | Java 17, Spring Boot 3.3.4            | REST API, 인증, 경매/알림/마이그레이션 처리          |
| 프론트엔드         | React 18, Redux Toolkit, React Router | 사용자 화면, 상태관리, API 호출                      |
| 실시간 입찰        | WebSocket                             | 경매방 단위 입찰 메시지 처리                         |
| 사용자 알림        | SSE                                   | 사용자별 알림 스트림 제공                            |
| 영속 저장소        | MariaDB                               | 사용자, 경매, 거래, 알림 등 관계형 데이터 저장       |
| 실시간/보조 저장소 | Redis                                 | 입찰 로그, refresh token, 일부 운영 보조 데이터 저장 |
| 모니터링           | Prometheus, Grafana, Actuator         | 헬스체크 및 메트릭 수집                              |
| 부하/보조 유틸     | `k6-test`, `server-health-check`      | 부하 테스트 스크립트와 서버 상태 확인                |

## 핵심 기능

### 인증과 사용자

- 로그인, 회원가입, 로그아웃, access token 재발급 흐름이 구현되어 있습니다.
- refresh token은 Redis에 저장하는 구조입니다.
- 인증이 필요한 API는 토큰이 없거나 유효하지 않으면 차단하는 방향으로 테스트가 작성되어 있습니다.
- 사용자 본인 정보 조회 기능이 있습니다.

### 경매

- 경매 목록, 추천 경매, 상세 조회 기능이 있습니다.
- 경매 등록은 multipart 기반으로 처리합니다.
- 관심 경매 토글 기능이 있습니다.
- 즉시구매 기능이 있으며, 현재 설명 기준으로는 HTTP 요청으로 처리하는 흐름이 중심입니다.

### 실시간 입찰과 알림

- 경매방 단위 WebSocket 연결을 통해 입찰 요청을 처리합니다.
- 알림은 SSE 스트림으로 사용자별 전달됩니다.
- 알림 목록 조회, 읽음 처리, 삭제 처리, 전체 읽음/삭제가 구현되어 있습니다.
- 종료 임박 알림과 구매 결과 알림 타입 분기까지 문서화되어 있습니다.

### 데이터 마이그레이션과 운영

- Redis와 MariaDB 사이의 입찰 데이터 마이그레이션 API가 있습니다.
- Actuator 기반 헬스체크와 Prometheus 메트릭 노출 구성이 있습니다.
- `server-health-check` 유틸이 서버 및 DB 상태를 확인하는 용도로 존재합니다.

## 테스트 현황

### 문서 기준 스냅샷

- 기존 문서 기준 대표 수치는 다음과 같습니다.
- 백엔드 단위 테스트: `49 passed`
- 백엔드 통합 테스트: `3 passed`
- 백엔드 시스템 테스트: `3 passed`
- 프론트 단위 테스트: 당시 문서 기준 `8 suites / 23 tests passed`

참고:

- 문서 작성 시점이 다르기 때문에 테스트 수치는 최신 워크스페이스와 완전히 같지 않을 수 있습니다.
- 이 문서에서는 숫자 자체보다 `어떤 기능이 자동 검증 대상인지`를 보는 것이 더 중요합니다.

### 현재 문서상 검증되는 범위

- 인증: 로그인, 회원가입, 로그아웃, 토큰 재발급, 입력 검증
- 사용자: 내 정보 조회, 인증 차단
- 카테고리: 목록 조회, Entity -> DTO 매핑
- 경매: 목록/추천/상세, 등록, 즉시구매, 관심 경매
- 알림: 목록, 읽음, 삭제, SSE 연결, 종료 임박 알림
- WebSocket: 토큰 없는 입찰 차단, 잘못된 `buy-now` 메시지 차단
- 마이그레이션: Redis/MariaDB 대상별 이관 흐름
- 프론트: 유틸, Redux 상태, API 래퍼, 일부 로그인/회원가입/라우팅/상세/알림 UI 테스트

### 아직 상대적으로 얇은 구간

- 실제 브라우저 기반 E2E 테스트
- 정상 입찰 성공 흐름의 종단간 WebSocket 시나리오
- 동시 입찰 경쟁 상황의 실운영 수준 검증
- 스케줄러/종료 처리/서버 복구 로직의 깊은 검증
- 성능, 부하, 모바일/네트워크 불안정 환경 검증
- 일부 프론트 페이지와 공통 UI 컴포넌트의 전용 테스트

## 핵심 리스크와 개선 우선순위

### 가장 중요한 리스크

- 현재 구조에는 세션/방 관리 수준의 동시성 완화는 존재합니다.
- 하지만 입찰 금액 갱신 자체를 `원자적 단일 연산`으로 보호하는 장치는 부족합니다.
- 즉, 현재 입찰 경로는 사실상 `조회 -> 검증 -> 저장` 순서이며, 동시 요청이 몰리면 경쟁 조건이 생길 수 있습니다.
- 그 결과 낮은 금액이 나중에 저장되거나, 동일 사용자/동일 경매의 중복 요청이 모두 승인될 가능성이 문서상 리스크로 정리되어 있습니다.

### 왜 중요한가

- 이 프로젝트의 차별점이 실시간 경매라서, 입찰 원자성 문제는 핵심 기능의 신뢰성과 직결됩니다.
- 문서도 이 부분을 가장 큰 기술 부채이자 우선 개선 항목으로 보고 있습니다.

### 우선순위별 개선 방향

1. 서버 입찰 경로에 경매 단위 원자 게이트를 도입합니다.
2. 현재가 갱신을 CAS 또는 조건부 갱신으로 바꿉니다.
3. 동일 요청 중복 처리를 위한 idempotency 키를 도입합니다.
4. 가격 갱신 성공 후에만 입찰 이력 저장/브로드캐스트가 일어나도록 순서를 정리합니다.
5. 충돌 시 최신 가격을 응답에 포함해 클라이언트가 즉시 재동기화할 수 있게 합니다.
6. 프론트에서 in-flight 제출 잠금으로 중복 클릭을 완화합니다.
7. 동시성 테스트와 부하 테스트를 CI 수준까지 확장합니다.

### 문서상 추가 과제

- `SecurityConfig`의 과도한 개방 설정 검토 필요
- `k6-test` 일부 경로와 실제 구현 정합성 점검 필요
- 레거시 SSE 흐름과 현재 알림 경로 정리 필요

## 작업 이력 요약

### 지금까지 한 일

1. 코드베이스 전체를 분석해 As-Is 명세 문서를 만들었습니다.
2. 입찰 동시성/원자성 리스크를 별도 문서로 정리했습니다.
3. 알림 정책과 종료 메시지 분기를 정리했습니다.
4. 즉시구매 요청 채널을 HTTP 중심으로 다시 설명하고 문서와 코드 흐름을 맞췄습니다.
5. 백엔드/프론트 테스트 범위를 넓히고 PASS 범위를 문서화했습니다.
6. 루트 README를 포트폴리오형 문서로 재정리했습니다.

### 최근 작업의 성격

- 최근 변화의 중심은 대규모 기능 추가보다 `문서 정합성`, `테스트 가시성`, `설명 품질 향상`에 가깝습니다.
- 즉, 구현만 있는 프로젝트가 아니라 “어떻게 설명하고 검증할 것인가”까지 정리한 상태라고 볼 수 있습니다.

## 설명 포인트

### 발표/면접에서 강조하기 좋은 부분

1. 실시간 입찰(WebSocket)과 사용자 알림(SSE)을 분리한 구조입니다.
2. 즉시구매는 요청 처리와 결과 전파를 분리해 설명할 수 있습니다.
3. 동시성 문제를 감추지 않고, 현재 완화 수준과 미해결 영역을 구분해서 설명할 수 있습니다.
4. 기능 구현뿐 아니라 문서화, 테스트, 운영 관점까지 함께 정리돼 있습니다.

### 짧게 설명하면

- “실시간 경매의 주요 흐름은 구현돼 있고 테스트도 어느 정도 갖춰져 있지만, 입찰 원자성은 아직 가장 중요한 미해결 과제로 남아 있는 프로젝트”라고 정리할 수 있습니다.

## 한 줄 요약

이 프로젝트는 `실시간 경매 핵심 기능 + 테스트 + 문서화`가 잘 정리된 편이지만, 가장 중요한 기술 과제는 여전히 `입찰 동시성/원자성 보장`입니다.
